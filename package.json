{
  "name": "divi-manager",
  "version": "1.0.0",
  "private": true,
  "description": "HTML to editable Divi 4 JSON and Divi 4 JSON responsive/CSS repair.",
  "scripts": {
    "check": "node --check core.js && node --check html-converter.js && node --check json-repair.js && node --check ui.js && node --check divi4-knowledge.js"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
